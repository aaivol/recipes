<p id="notice"><%= notice %></p>

<p>
  <strong>Username:</strong>
  <%= @user.username %>
</p>

<p>
  <strong>Last login at:</strong>
  <%= @user.last_login_at %>
</p>

<% if current_user == @user %>
  <%= link_to t('users.index.link_update'), edit_user_path(@user) %>
<% end %>
|
<%= link_to t('users.index.users_'), users_path %>